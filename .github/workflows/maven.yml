name: Executar GitAction

on:
  workflow_dispatch:
    inputs:
      project_dir:
        description: "Diretório do projeto (contexto do Docker)"
        required: true
        default: "."
      module_gav:
        description: "GAV alvo (GroupId, ArtifactId, Version)"
        required: false
        default: ""

permissions:
  contents: read
  packages: write

env:
  # Reutilizáveis
  JAVA_VERSION: "24"
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  DOCKER_PLATFORMS: linux/amd64

  # Maven (padrão: usar o ator que executa o workflow)
  MAVEN_USERNAME: ${{ github.actor }}
  MAVEN_PASSWORD: ${{ secrets.GH_PACKAGES_TOKEN }}
  MAVEN_SERVER_ID: github-ramir-util

  # Qualidade / Telemetria
  SONAR_ORG: ramiralvesmelo
  SONAR_PROJECT_KEY: ramiralvesmelo_app-audit
  CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

  # Inputs do dispatch (disponíveis globalmente)
  PROJECT_DIR: ${{ github.event.inputs.project_dir }}
  MODULE_GAV: ${{ github.event.inputs.module_gav }}

  # Docker Hub (VARIÁVELS DE AMBIENTE DEFINIDA COMO SECRET)
  DOCKERHUB_REGISTRY: docker.io
  DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
  DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
  DOCKERHUB_IMAGE: ramiralvesmelo/app-audit

jobs:

  ci:
    name: "🔍 CI → Análise de Código & Cobertura"
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    steps:
      - name: "📥 Checkout do repositório"
        uses: actions/checkout@v4

      - name: "🧹 Remover jacocoArgLine vazio do pom.xml"
        run: |
          sed -i 's|<jacocoArgLine></jacocoArgLine>||g' pom.xml
          echo "✅ jacocoArgLine removido do pom.xml"

      - name: "⚙️ JDK ${{ env.JAVA_VERSION }} + cache Maven + credencial"
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}
          cache: maven
          check-latest: true
          server-id: ${{ env.MAVEN_SERVER_ID }}
          server-username: MAVEN_USERNAME
          server-password: MAVEN_PASSWORD

      - name: "🔎 Verificar Versão Java | Maven"
        run: |
          java -version
          mvn -v
          echo "Mostrando servers do settings (mascarado):"
          sed 's|<password>.*</password>|<password>***</password>|' ~/.m2/settings.xml | head -n 40

      - name: "🛠️ Build + Test + Coverage (jacoco.xml)"
        working-directory: ${{ env.PROJECT_DIR }}
        run: |
          set -e
          if [ -n "${MODULE_GAV}" ]; then
            mvn -B -V -s ~/.m2/settings.xml -DskipTests=false clean verify -am -pl "${MODULE_GAV}"
          else
            mvn -B -V -s ~/.m2/settings.xml -DskipTests=false clean verify
          fi
          echo "Procurando jacoco.xml…"
          find . -type f -path "*/target/site/jacoco/jacoco.xml" -printf "%p\n" || true
          test -n "$(find . -type f -path "*/target/site/jacoco/jacoco.xml" -print -quit)" || { echo "jacoco.xml não encontrado"; exit 1; }

      - name: "📈 Enviar cobertura para o Codecov"
        if: ${{ !cancelled() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false) }}
        uses: codecov/codecov-action@v4
        with:
          token: ${{ env.CODECOV_TOKEN }}
          files: "**/target/site/jacoco/jacoco.xml"
          slug: ${{ github.repository }}
          fail_ci_if_error: true
          verbose: false

      - name: "🔎 SonarCloud (Aguarda Quality Gate)"
        if: ${{ github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false }}
        working-directory: ${{ env.PROJECT_DIR }}
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          mvn -B -V -s ~/.m2/settings.xml \
            -Dsonar.host.url=https://sonarcloud.io \
            -Dsonar.login="${SONAR_TOKEN}" \
            -Dsonar.organization="${SONAR_ORG}" \
            -Dsonar.projectKey="${SONAR_PROJECT_KEY}" \
            -Dsonar.coverage.jacoco.xmlReportPaths="**/target/site/jacoco/jacoco.xml" \
            -Dsonar.qualitygate.wait=true \
            org.sonarsource.scanner.maven:sonar-maven-plugin:sonar

      - name: "📦 Gerar SBOM (CycloneDX)"
        working-directory: ${{ env.PROJECT_DIR }}
        run: mvn -B -s ~/.m2/settings.xml cyclonedx:makeAggregateBom --no-transfer-progress

      - name: "📤 Publicar SBOM como artefato"
        uses: actions/upload-artifact@v4
        with:
          name: sbom-cyclonedx
          path: |
            ${{ env.PROJECT_DIR }}/target/bom.xml
            ${{ env.PROJECT_DIR }}/target/bom.json
            ${{ env.PROJECT_DIR }}/target/*-cyclonedx.xml
            ${{ env.PROJECT_DIR }}/target/*-cyclonedx.json
          if-no-files-found: error
          overwrite: true

      - name: "📊 Enviar snapshot de dependências (Dependabot/Graph)"
        if: ${{ github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false }}
        uses: advanced-security/maven-dependency-submission-action@v4

  cd:
    name: "🚀 CD → Imagem Docker & Deploy (GHCR)"
    runs-on: ubuntu-latest
    needs: ci
    if: ${{ github.event_name != 'pull_request' }}
    permissions:
      contents: write
      packages: write
    steps:
      - name: "📥 Checkout"
        uses: actions/checkout@v4

      - name: "🧱 Docker Buildx (setup)"
        uses: docker/setup-buildx-action@v3

      - name: "🔑 Login no GHCR"
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: "🔑 Login no Docker Hub"
        uses: docker/login-action@v3
        with:
          registry: ${{ env.DOCKERHUB_REGISTRY }}
          username: ${{ env.DOCKERHUB_USERNAME }}
          password: ${{ env.DOCKERHUB_TOKEN }}

      - name: "🏷️ Metadados da imagem (tags/labels)"
        id: meta
        uses: docker/metadata-action@v5
        with:
          # PUSH IMAGE GHCR + Docker Hub
          images: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
            ${{ env.DOCKERHUB_IMAGE }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,format=short,prefix=sha-
            type=ref,event=branch
            type=ref,event=tag

      - name: "🧾 Criar maven-settings.xml (arquivo)"
        id: prep-settings
        shell: bash
        run: |
          set -euo pipefail
          cat > "$RUNNER_TEMP/maven-settings.xml" <<'XML'
          <?xml version="1.0" encoding="UTF-8"?>
          <settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
                    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                    xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 https://maven.apache.org/xsd/settings-1.0.0.xsd">
            <servers>
              <server>
                <id>${MAVEN_SERVER_ID}</id>
                <username>${MAVEN_USERNAME}</username>
                <password>${MAVEN_PASSWORD}</password>
              </server>
            </servers>
          </settings>
          XML
          sed -i 's|${MAVEN_SERVER_ID}|'"${MAVEN_SERVER_ID}"'|g' "$RUNNER_TEMP/maven-settings.xml"
          sed -i 's|${MAVEN_USERNAME}|'"${MAVEN_USERNAME}"'|g' "$RUNNER_TEMP/maven-settings.xml"
          sed -i 's|${MAVEN_PASSWORD}|'"${MAVEN_PASSWORD}"'|g' "$RUNNER_TEMP/maven-settings.xml"
          test -s "$RUNNER_TEMP/maven-settings.xml"
          echo "path=$RUNNER_TEMP/maven-settings.xml" >> "$GITHUB_OUTPUT"

      - name: "👀 Conferir settings (mascarado)"
        shell: bash
        run: |
          ls -l "${{ steps.prep-settings.outputs.path }}"
          sed 's|\(<password>\).*\(</password>\)|\1***\2|' "${{ steps.prep-settings.outputs.path }}" | head -n 20

      - name: "🐳 Build & Push (GHCR & Docker Hub)"
        uses: docker/build-push-action@v6
        with:
          context: ${{ env.PROJECT_DIR }}
          file: ${{ env.PROJECT_DIR }}/infra/docker/Dockerfile
          push: true
          platforms: ${{ env.DOCKER_PLATFORMS }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          secret-files: |
            maven_settings=${{ steps.prep-settings.outputs.path }}
          secrets: |
            id=gh_user,env=MAVEN_USERNAME
            id=gh_pat,env=MAVEN_PASSWORD
          cache-from: type=gha
          cache-to: type=gha,mode=max
